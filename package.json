{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "university request detection",
  "main": "worker.js",
  "scripts": {
    "test": "mocha"
  },
  "author": "{{ authors }}",
  "devDependencies": {
    "@dollarshaveclub/cloudworker": "^0.1.2",
    "mocha": "^8.2.1"
  }
}
